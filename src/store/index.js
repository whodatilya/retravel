import { createStore } from 'vuex'
import user from './user'
import posts from './posts'
import map from './map'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user,
    posts,
    map
  }
})
